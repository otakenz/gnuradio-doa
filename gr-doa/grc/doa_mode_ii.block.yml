# auto-generated by grc.converter

id: doa_mode_ii
label: Mode
category: '[doa]'

parameters:
-   id: vector_size
    label: Vector Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: max_value
    label: Max Value
    dtype: int

inputs:
-   domain: stream
    dtype: int
    vlen: ${ vector_size }

outputs:
-   domain: stream
    dtype: int

templates:
    imports: import doa
    make: doa.mode_ii(${vector_size}, ${max_value})

file_format: 1
