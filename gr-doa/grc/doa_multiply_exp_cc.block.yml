# auto-generated by grc.converter

id: doa_multiply_exp_cc
label: Multiply Exp
category: '[doa]'

parameters:
-   id: arg
    label: Argument
    dtype: float

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: arg
    optional: true

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import doa
    make: doa.multiply_exp_cc(${arg})
    callbacks:
    - set_arg(${arg})

file_format: 1
