# auto-generated by grc.converter

id: doa_full_capon3_ccf
label: Full Capon 3
category: '[doa]'

parameters:
-   id: vec_in
    label: Vector In
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vec_out
    label: Vector Out
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   label: in0
    domain: stream
    dtype: complex
    vlen: ${ vec_in }
-   label: in1
    domain: stream
    dtype: complex
    vlen: ${ vec_in }
-   label: in2
    domain: stream
    dtype: complex
    vlen: ${ vec_in }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vec_out }

templates:
    imports: import doa
    make: doa.full_capon3_ccf(${vec_in}, ${vec_out})

file_format: 1
