# auto-generated by grc.converter

id: doa_hold_ii
label: Hold
category: '[doa]'

parameters:
-   id: hold
    label: Hold
    dtype: bool

inputs:
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: int

templates:
    imports: import doa
    make: doa.hold_ii(${hold})
    callbacks:
    - set_hold(${hold})

file_format: 1
