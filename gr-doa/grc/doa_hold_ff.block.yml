# auto-generated by grc.converter

id: doa_hold_ff
label: Hold
category: '[doa]'

parameters:
-   id: hold
    label: Hold
    dtype: bool

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import doa
    make: doa.hold_ff(${hold})
    callbacks:
    - set_hold(${hold})

file_format: 1
