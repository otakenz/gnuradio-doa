# auto-generated by grc.converter

id: doa_hold_state_ff
label: Hold State
category: '[doa]'

parameters:
-   id: state
    label: State
    dtype: int
    default: '0'
-   id: scale0
    label: Scale 0
    dtype: float
    default: '-1'
-   id: scale1
    label: Scale 1
    dtype: float
    default: '0'

inputs:
-   domain: stream
    dtype: float
-   domain: message
    id: state
    optional: true

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import doa
    make: doa.hold_state_ff(${state}, ${scale0}, ${scale1})

file_format: 1
