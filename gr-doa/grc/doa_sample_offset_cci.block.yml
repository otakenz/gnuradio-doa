# auto-generated by grc.converter

id: doa_sample_offset_cci
label: Sample Offset
category: '[doa]'

parameters:
-   id: vector_len
    label: Vector Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vector_len == 1 else 'none' }
-   id: num_xcorr
    label: Iterations
    dtype: int
    default: '1'
-   id: showports
    label: Show Msg Ports
    dtype: enum
    default: 'True'
    options: ['False', 'True']
    option_labels: ['Yes', 'No']
    hide: part

inputs:
-   label: in0
    domain: stream
    dtype: complex
    vlen: ${ vector_len }
-   label: in1
    domain: stream
    dtype: complex
    vlen: ${ vector_len }
-   domain: message
    id: recalc
    optional: true
    hide: ${ showports }

outputs:
-   domain: stream
    dtype: int

templates:
    imports: import doa
    make: doa.sample_offset_cci(${vector_len}, ${num_xcorr})

file_format: 1
