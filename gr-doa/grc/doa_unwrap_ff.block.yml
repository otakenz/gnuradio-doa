# auto-generated by grc.converter

id: doa_unwrap_ff
label: Unwrap
category: '[doa]'

parameters:
-   id: vector_size
    label: Vector Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: min
    label: Min
    dtype: float
    default: '-3.141593'
-   id: max
    label: Max
    dtype: float
    default: '3.141593'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vector_size }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vector_size }

templates:
    imports: import doa
    make: doa.unwrap_ff(${vector_size}, ${min}, ${max})

file_format: 1
