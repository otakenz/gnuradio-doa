# auto-generated by grc.converter

id: doa_lin_delay_cc
label: Linear Align
category: '[doa]'

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: int
-   id: delay
    label: Delay
    dtype: float
    default: '0'

inputs:
-   label: ref
    domain: stream
    dtype: complex
-   domain: stream
    dtype: complex

outputs:
-   label: ref
    domain: stream
    dtype: complex
-   domain: stream
    dtype: complex

templates:
    imports: import doa
    make: doa.lin_delay_cc(${samp_rate}, ${delay})
    callbacks:
    - set_dly(${delay})

file_format: 1
