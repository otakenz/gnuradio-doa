# auto-generated by grc.converter

id: doa_zero_pad_cc
label: Zero Pad
category: '[doa]'

parameters:
-   id: vec_len_in
    label: Vector In Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vec_len_out
    label: Vector Out Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vec_len_in }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vec_len_out }

templates:
    imports: import doa
    make: doa.zero_pad_cc(${vec_len_in}, ${vec_len_out})

file_format: 1
