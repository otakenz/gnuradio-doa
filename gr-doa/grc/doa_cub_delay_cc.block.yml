# auto-generated by grc.converter

id: doa_cub_delay_cc
label: Cubic Align
category: '[doa]'

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: int
-   id: delay
    label: Delay
    dtype: float
    default: '0'
-   id: showports
    label: Show Msg Ports
    dtype: enum
    default: 'True'
    options: ['False', 'True']
    option_labels: ['Yes', 'No']
    hide: part

inputs:
-   label: ref
    domain: stream
    dtype: complex
-   domain: stream
    dtype: complex
-   domain: message
    id: reset
    optional: true
    hide: ${ showports }

outputs:
-   label: ref
    domain: stream
    dtype: complex
-   domain: stream
    dtype: complex

templates:
    imports: import doa
    make: doa.cub_delay_cc(${samp_rate}, ${delay})
    callbacks:
    - set_dly(${delay})

file_format: 1
