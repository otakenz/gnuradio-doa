# auto-generated by grc.converter

id: doa_cumulative_ff
label: Cumulative Sum
category: '[doa]'

parameters:
-   id: start
    label: Default Value
    dtype: float
    default: '0'
-   id: showports
    label: Show Msg Ports
    dtype: enum
    default: 'True'
    options: ['False', 'True']
    option_labels: ['Yes', 'No']
    hide: part

inputs:
-   domain: stream
    dtype: float
-   domain: message
    id: reset
    optional: true
    hide: ${ showports }

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import doa
    make: doa.cumulative_ff(${start})

file_format: 1
