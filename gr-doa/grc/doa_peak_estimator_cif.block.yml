# auto-generated by grc.converter

id: doa_peak_estimator_cif
label: Peak Estimator
category: '[doa]'

parameters:
-   id: vector_len
    label: Vector Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vector_len }

outputs:
-   label: bin
    domain: stream
    dtype: int
-   label: delta
    domain: stream
    dtype: float

templates:
    imports: import doa
    make: doa.peak_estimator_cif(${vector_len})

file_format: 1
